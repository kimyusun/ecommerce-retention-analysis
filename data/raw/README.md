# Raw Data

이 폴더는 원본 CSV를 로컬에 배치하는 위치다.

공개 GitHub 레포에는 부트캠프에서 제공받은 원본 거래 데이터 `ecommerce_data.csv`를 포함하지 않는다. 분석을 재현하려면 로컬에서 아래 경로에 파일을 둔다.

```text
data/raw/ecommerce_data.csv
```

포트폴리오 문서와 대시보드에는 원본 데이터를 재계산해 만든 파생 요약 지표만 포함한다.

